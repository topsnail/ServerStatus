# 复制为项目根目录 .env：cp deploy/env.example .env
# 然后改掉 ADMIN_TOKEN（务必用强随机串）

# 管理 API / WebUI「配置」页 Bearer Token（必填生产）
# 生成示例：openssl rand -hex 32
ADMIN_TOKEN=请改成强随机串

# 容器时区
TZ=Asia/Shanghai

# 可选：Watchdog/证书回调允许不可信 TLS（一般保持 false）
INSECURE_CALLBACK_TLS=false

# 可选：输出 Gin HTTP 请求日志
VERBOSE=false
